{
  "name": "crayon",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "eslint ./**/*.js --fix",
    "generate": "node ./dev/generate-readme",
    "make:command": "node ./dev/make-command",
    "test": "jest tests/"
  },
  "dependencies": {
    "caporal": "^0.10.0",
    "fs-extra": "^5.0.0",
    "glob": "^7.1.2",
    "inquirer": "^5.1.0",
    "lodash": "^4.17.5"
  },
  "devDependencies": {
    "eslint": "^4.10.0",
    "eslint-config-netsells": "github:netsells/eslint-config-netsells",
    "husky": "^0.15.0-rc.8",
    "jest": "^22.4.2",
    "slug": "^0.9.1"
  },
  "bin": {
    "crayon": "./index.js"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint && npm run generate && git add README.md"
    }
  }
}
